enum RoutineCategory { work, exercise, health, personal, study, others }

enum RoutinePriority { high, medium, low }

enum RoutineStatus { pending, completed, skipped }

enum RepeatType { none, daily, weekly, custom }
